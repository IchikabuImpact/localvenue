# Database Context

このファイルは公開リポジトリ向けに、実データを含まない案内だけを残しています。

- スキーマは `data/schema.sql` を参照してください。
- 初期マスターデータは `data/seed-master.sql` を参照してください。
- 実DBから生成したテーブル内容、レースデータ、予想データ、払戻データなどは公開リポジトリへコミットしないでください。
- ローカル検証でDBコンテキストが必要な場合は、各自の環境で `npm run db:dump` を実行して生成し、コミット前に差分から除外してください。
